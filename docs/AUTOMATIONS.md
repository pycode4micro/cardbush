# 定时与事件自动化

侧栏「定时与自动化」和 agent 的 `schedule_task` 使用同一个 Runtime 调度器和持久化任务表。定时任务到点后在独立临时会话执行；会话事件和受信任插件 Hook 仍关联来源会话。结果、工具记录、授权请求和停止动作复用现有会话链路。

## 日历与计划管理

默认打开「日历」，入口位于「未读」之前。支持日/月/年切换、前后翻页、回到今天及方向键导航。日视图集中显示当天安排；月视图每格只保留一个名称和剩余数量，宽窗口右侧显示明细，窄窗口改到下方；年视图显示 12 个月的日期分布，点击月份进入月视图。

点击年月标题或「今天」打开日期范围选择器，可直接输入年份并选择某天、某月或某年；「回到今天」位于弹层底部。支持方向键、Esc、点击外部关闭及焦点恢复。弹层使用应用语义主题变量，随浅色、深色、赛博朋克及导入配色实时变化。保留搜索条件，所选范围没有匹配安排时提示，不清空筛选。

搜索名称、提示词、导入日历标题/说明或 ISO 日期时，自动定位到匹配日期；月视图只保留匹配的日期，年视图只保留有结果的月份。已有历史、单次未来计划和导入数据可跨年定位；无限周期按所浏览年份按需投影，不生成永久日期副本。

「日历数据」内置中国农历显示，并支持 JSON/ICS 导入、启停和移除。导入数据只供展示，不创建任务、不注入模型上下文；保存在用户数据目录 `calendars/calendars.json`，失败不覆盖旧数据。同 ID 重导会替换原数据并保留启用状态。解析由有内存上限的 Worker 承担，5 秒超时、单文件 8 MiB；最多 30 个数据集、总计 50,000 条、16 MiB 存储。年视图用区间计数，月视图保留有限预览，当天明细每次追加 100 条，避免展开大量跨日事件。协议、ICS 展开范围及闰月转换说明见内置 [cardbush-docs 日历协议](../assets/skills/cardbush-docs/references/calendar-protocol.md)。

计划管理和当天列表都逐层展开：默认显示最多 14 个字的名称、15 个字的提示词预览以及时间和状态，点击展开完整提示词与管理操作，再展开执行记录。搜索、编辑与执行始终使用完整原文，截短只影响显示。

任务安排直接从现有调度快照派生，不保存第二份计划或改变执行规则。未来只展示已启用且有 `nextRunAt` 的定时计划，按固定间隔预计后续时间；高频安排合并为当天的时间范围和次数，不逐次生成大量条目。过期重复计划只保留调度器当前待执行的一次，不补造错过的执行。历史使用保留的真实记录，按排队日期归日，完成跨日不会移动原记录；暂停计划保留历史但不生成未来安排。任务日期按本机时区显示，间隔仍按实际经过的秒数计算，覆盖夏令时边界；JSON 导入显示其声明时区的民用日期，ICS 带时间的事件在导入时转换到设备时区。

事件触发计划没有固定日期，日历下方提供计划管理入口；已有执行记录照常出现在实际日期。点击历史继续使用现有执行面板，不自动标记已读。`npm run test:calendar` 覆盖日期与调度投影、5 万跨日条目、真实导入 Worker、持久化与失败保护、农历转换、日/月/年搜索交互以及浅色、深色和窄窗口。

## 使用

1. 在目标会话发送一条消息，建立模型、工作区、工具目录和权限配置。
2. 打开「定时与自动化」，填写名称、目标会话、执行提示词与触发方式。也可以在会话中要求 agent 设置定时。
3. 支持指定时间、固定重复间隔（至少一分钟）、任务完成、工具成功和工具失败。工具事件可填写精确的 Runtime 工具名，留空匹配全部；冷却时间至少一分钟。
4. 可查看执行记录、打开原会话、编辑、暂停、启用、立即执行、停止和删除。运行中的任务需先停止才能编辑或删除；暂停会取消排队，停止还会中止正在执行的一轮。

日期输入和列表时间按本机时区显示，实际存储带时区偏移的 ISO 时间；固定间隔按经过的秒数重复，不是 Cron 或按夏令时变化的日历规则。UI 使用当前本机时区保存，工具可显式传入 `time_zone`。

工具示例：

```json
{
  "action": "create",
  "name": "项目检查",
  "prompt": "检查项目构建结果，整理失败原因和需要处理的问题。",
  "time_zone": "Asia/Shanghai",
  "trigger": { "kind": "interval", "at": "2026-09-10T09:00:00+08:00", "seconds": 86400 }
}
```

`list` 返回本会话的任务。`update` 传完整定义；其他管理操作使用 `job_id`。可携带 `expected_revision` 拒绝并发覆盖。`cancel` 为 `pause` 的别名。子 agent 不开放该工具。

## 原生 Hook 唤醒

已审核并信任的 command、MCP Tool 或 HTTP Hook 可在成功的结构化输出中返回：

```json
{
  "cardbush": {
    "activateAgent": {
      "prompt": "检查刚刚导出的文件，整理结果并继续完成用户的任务。"
    }
  }
}
```

command 使用 stdout JSON；MCP 使用 `structuredContent`，或可解析为 JSON 的文本输出。该字段可以与原有 Hook 输出字段并存。提示词最长 32,000 字符，绑定当前会话。前一轮未结束时先排队，之后使用现有 Runtime 启动新一轮，不转换为 Skill。

这是 **CardBush 的唤醒扩展**，不等同于 Claude 的 `type: prompt` / `type: agent` 审查器语义。后两类现在通过独立模型任务判断事件，agent 仅提供文件读取和搜索；OpenAI 来源的 prompt / agent 仍按其规范跳过。具体调用、权限和超时边界见[插件兼容说明](PLUGIN_COMPATIBILITY.md)。

唤醒任务记录插件 ID、Hook ID 与审核时的定义哈希。开始执行前重新确认插件仍启用、定义未变且仍受信任。相同 Hook 在同一会话复用一个任务和执行历史；暂停该任务会抑制后续唤醒，直到重新启用。用户编辑插件唤醒任务会将其转为普通手动定义的自动化；后续插件唤醒可重新生成独立的插件任务。

## 执行与恢复

- 定时任务在创建或明确保存时，从来源会话保存模型、工具范围、生成选项、工作区和权限；执行使用任务自己的配置，不携带来源聊天记录。提示词必须包含完整要求。删除来源会话不取消定时任务，也不删除其执行结果；仍可编辑任务，来源不存在时保留已保存配置。事件任务和插件唤醒仍依赖来源，删除来源会暂停并取消尚未执行的唤醒。
- 临时执行默认不进入最近会话，结果在日历、未读和全部结果中查看。结果侧栏保留「返回来源会话」；来源已删除时明确显示。点击「打开执行会话」才将本次执行放入最近列表。删除会话前显示确认及影响说明。
- 模型连接在执行前经 Product Host 重新解析；任务文件不保存 API Key，公开列表不返回系统提示词或执行配置。旧定时任务迁移未来执行，不改写历史和原提示词，不重新启用手动暂停的任务；配置已丢失时明确报错。
- 自动执行保留来源会话的工具排列，并核对当前真实定义和可用性；触发信息作为新消息写入本轮历史，使用执行记录中的 ID 和时间，不插入固定前缀。未读状态变化只追加观察记录，原消息保持不变；参数保存与模型请求共用生成选项定义。详见 [请求与提醒连续性](AUTOMATION_INBOX.md#request-continuity)。
- 不提升权限。需要授权时沿用现有交互处理；模型已移除、插件撤销信任等失败会暂停任务并显示原因。
- 同一执行会话串行，全局最多三个任务同时运行。独立定时任务不等待来源会话空闲，仍受 Runtime 资源准入控制。前台消息抢占同一执行会话时自动任务重新排队。工具事件发生后也等原轮次释放会话；任务完成事件只在真正完成后发布，失败或中间 Stop 尝试不冒充完成。
- 重复事件去重，冷却期或已有排队/执行中的重复事件合并。自动执行和子 agent 产生的事件不会触发自动化或插件唤醒，避免递归循环。
- 应用必须运行；不安装操作系统定时任务，也不唤醒已关闭的应用。休眠或关闭期间错过的执行恢复后合并一次；执行中断且结果未确认的任务暂停，保留中断记录，不自动重放副作用。
- 配置保存在 `runtime-state/scheduler/automations.json`，原子写入；损坏时明确报错，不覆盖原文件。每个任务保留最近 50 次记录，最多 500 个任务。旧版 `scheduler/jobs.json` 仅为未接入执行的占位记录，不自动迁移激活。
- 当前事件源仅为会话生命周期。文件监听、Webhook、Slack 等外部事件订阅及 Cron / RRULE 尚未接入。

`npm run test:automations` 覆盖调度、忙碌竞争、去重、停止、中断恢复、Hook 唤醒、真实 Electron 进程重启与模型连接恢复、原会话记录，以及界面管理和窄窗口布局。测试使用隔离目录和本机模拟模型，不调用真实账户。
